# Same-origin — CF Pages Functions proxy (functions/[[path]].js) forwards
# /auth/v2/* to Better-Auth and /api/v1/* to Rails, injecting the session
# token from the cookie as Bearer.
VITE_API_URL=
VITE_BETTER_AUTH_URL=/auth/v2
