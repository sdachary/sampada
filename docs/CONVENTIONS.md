# Sampada — Coding Conventions

> Auto-loaded by OpenCode at session start. Last updated: 2026-06-20

---

## Code Style

| Rule | Convention |
|------|-----------|
| Indentation | Spaces, 2 |
| Naming (variables) | snake_case |
| Naming (files) | snake_case |
| Naming (classes) | PascalCase |
| Max line length | 120 |
| Quotes | Single (unless interpolation) |
| Semicolons | None (Ruby) |

---

## Imports

Rails autoloading via `Zeitwerk`. Explicit `require` only for library code. Gem dependencies in `Gemfile` grouped by environment.

---

## Testing

| Aspect | Convention |
|--------|-----------|
| Framework | RSpec |
| File naming | `*_spec.rb` |
| Location | `spec/` mirrors `app/` |
| Factories | `spec/factories/` (FactoryBot) |
| Run command | `bundle exec rspec` |

---

## Git

| Rule | Convention |
|------|-----------|
| Branch naming | `phase-XX-description` |
| Commit style | `Phase XX: description` |
| PR template | None |

---

## Error Handling

Standard Rails `rescue_from` in `ApplicationController`. JSON API errors return `{ error: message }` with appropriate HTTP status. Sidekiq job failures auto-retry (configurable). Use `Rails.logger` for warnings/errors. No custom error classes unless necessary.

---

## Environment Variables

| Variable | Required? | Purpose |
|----------|-----------|---------|
| `GOOGLE_OAUTH_CLIENT_ID` | Yes | Google OAuth |
| `GOOGLE_OAUTH_CLIENT_SECRET` | Yes | Google OAuth |
| `DATABASE_URL` | Yes | PostgreSQL connection |
| `REDIS_URL` | Yes | Redis/Sidekiq connection |
| `SECRET_KEY_BASE` | Yes | Rails secret key base |
| `DPO_EMAIL` | Yes | dpo@sampada.app |

---

## Files to Avoid Editing

- `db/schema.rb` — auto-generated by migrations
- `vendor/`, `node_modules/` — third-party
